stages:
  train:
    cmd: python -m textbench.pipeline
    deps:
      - textbench/pipeline.py
      - examples/texts.csv
    outs:
      - artifacts/model.joblib
    metrics:
      - artifacts/metrics.json
